// Describe what to test
describe('Sample Test', () => {
  // Test scenario
  it('runs jest', () => {
    expect(5).toBe(5)
  })
  // Test scenario
  it('checks array', () => {
    expect([1, 2, 3]).toEqual([1, 2, 3])
  })
})
